// Ottaly 2.0 splash — DISABLED 2026-06-17 (Jesse: the ~5s cover screen on every
// reload was annoying). Kept as a no-op so the <script src="/splash.js"> tags on
// all ~31 pages still resolve (no 404s) without showing anything. To bring it
// back, restore from git history (commit before this change).
(function ottalySplashDisabled() { /* intentionally does nothing */ })();
